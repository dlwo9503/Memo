<!DOCTYPE html> // 문서 타입이 html로 이루어져있다.

<head></head>

<body></body>

<h1></h1> // 1 ~ 6까지 존재
<br> // 한줄 내리기
<strong></> // 굵게
<u></u> // 밑줄
<p></p> // 단락 정하기
<li></li> // 리스트
<ul></li> // 리스트 감싸기
<ol></ol> // 리스트 점말고 넘버링
<a href = "주소"></a> 링크 걸기

<meta charset="UTF-8"> // 한글 깨질 때

<title></title> // 탭 제목

<script></script> // 스크립트 명령어 사용시에 사용

onclick="" // 클릭시
onchange="" // 값 변경시
onkeydown ="" // 키를 누를시

<div></div> // 태그로 묶기(줄바뀜)
<span></span> // 태그로 묶기(줄 안바뀜)

class = .
id = #

우선순위 : tag(span, div, h1 etc...) < class < id

class는 그룹핑하는거임 (중복 가능)
id는 식별하기위한거임 (중복 불가능)

document.querySelector('body').style.background = '색상'; // body태그 배경색 변경
document.querySelector('body').style.color = '색상'; // body태그 폰트 색상 

this.value = '값'; // 자기자신의 태그 값을 가르킴

document.querySelector('태그 이름') // 태그 이름과 같은 처음오는 태그를 가져옴
document.querySelectorAll('태그 이름') // 태그 이름과 같은 태그를 모두 가져와서 리스트 형태로 표현됨

console.log(); // 로그 확인

---------------------------------------------------

<img src = "">
style = "margin-top:40px"
width = "100%" // 너비
@media(min-width:800px) // 반응형 웹
<link rel="stylesheet" href="style.css"> // 밖에있는 css 스타일 사용
