버블 정렬 : 항상O(N^2)

선택 정렬 : 항상 O(N^2), 버블 정렬보다 조금 더 빠름

퀵 정렬 : O(NlogN) ~ O(N^2), 기준값에 따라 영향을 받음

힙 정렬 : 항상 O(NlogN), 근데 보통 퀵 정렬보다 느림

삽입 정렬 : O(N) ~ O(N^2), 성능의 편차가 심함

병합 정렬 : 항상 O(NlogN), 기준값에 영향을 안받음

쉘 정렬 :  O(N) ~ O(N^2), 삽입 정렬의 단점을 보완


번외

+ 기수 정렬 O(N)

+ 카운팅 정렬  O(N)
